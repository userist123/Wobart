'use client'

/**
 * WOB ART currently uses native scrolling by design. Heavy smooth-scroll
 * hijacking is intentionally avoided for accessibility and touch behavior.
 */
export function LenisProvider() {
  return null
}
